# Lab

Espacio de laboratorio de Pablo Zarate.

Cada proyecto vive en una carpeta con slug estable. Ese slug define la ruta
`/lab/<project-slug>` dentro de `webpz` y la URL publica
`https://lab.pablozarate.com/<project-slug>`.

## Projects

- `shader-experiment-01`: interactive PabloZarate wordmark shader with
  RAMS-aligned dark-mode grid, typography, and controls.
